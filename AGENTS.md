# AGENTS.md

本文件用于约束 Codex 或其他 AI 工具在本仓库中的开发行为，确保项目始终围绕授权安全场景、教学演示和本地靶场环境推进。

## 1. 项目背景

- 本项目是中国大学生信息安全大赛作品赛项目。
- 项目目标是实现面向授权安全场景的自主安全智能体。
- 所有安全相关能力默认仅用于授权环境、本地靶场、测试数据、安全运维辅助、应急响应分析、代码审计辅助和教学演示。
- 仓库中的 mock、示例命令和测试数据只用于最小可用链路验证，不代表真实攻击能力。

## 2. 开发边界

- 不允许实现未授权攻击功能。
- 不允许生成绕过审计、隐藏痕迹、持久化后门、真实攻击扩散等高风险逻辑。
- 不允许硬编码 API Key、Token、密码、Cookie、私钥等敏感信息。
- 不允许在 E 模块中直接执行危险命令。
- 所有命令执行必须通过后续沙箱或白名单机制接入，Execution 节点需要明确记录工具、参数、输出、错误和退出码。
- 涉及扫描、验证、审计的逻辑必须默认面向本地靶场、授权资产或测试数据。
- 对模糊或疑似越界的任务，应优先要求补充授权范围、目标信息和测试边界。

## 3. 代码规范

- Python 代码尽量简单、可读、类型清晰。
- 模块之间通过字典形式的 `state` 传递信息，方便后续与 LangGraph 集成。
- 节点函数建议采用 `xxx_node(state: dict) -> dict` 形式，并只读写约定字段。
- mock 逻辑要明确标注，方便后续替换为真实后端、真实 Execution 输出或 LLM 分析逻辑。
- 前端先使用 Streamlit 实现极简版本，不追求复杂 UI。
- 不做大规模重构，不改动其他同学核心模块逻辑。
- 新增逻辑应优先保证可运行、可联调、可解释。

## 4. 文档规范

- `docs/` 下维护接口约定、测试用例、E 模块交付说明和每日进展记录。
- 文档要写清楚输入、输出、字段含义和后续对接方式。
- 测试文档应覆盖正常输入、模糊输入、危险输入和多用户状态隔离等关键场景。
- 交付说明应明确当前哪些能力是 mock，哪些能力后续需要替换或扩展。
